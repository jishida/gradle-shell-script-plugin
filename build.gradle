buildscript {
    repositories {
        maven { url 'https://plugins.gradle.org/m2/' }
    }

    dependencies {
        classpath group: 'com.gradle.publish', name: 'plugin-publish-plugin', version: '0.9.6'
        classpath group: 'com.jfrog.bintray.gradle', name: 'gradle-bintray-plugin', version: '1.7.2'
    }
}

group 'com.github.jishida.gradle'

apply plugin: 'groovy'
apply from: file('gradle/integration-test.gradle')
apply from: file('gradle/publishing.gradle')

sourceCompatibility = compatibilityVersion
targetCompatibility = compatibilityVersion

repositories {
    mavenCentral()
}

dependencies {
    compile gradleApi()
    compile group: 'org.tukaani', name: 'xz', version: '1.5'
    testCompile group: 'com.netflix.nebula', name: 'nebula-test', version: '4.2.2'
}